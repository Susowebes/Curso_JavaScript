//Creamos un objeto con 2 propiedades.
let coche={color:"rojo",potencia:400};

// Metodo para ver el código directamente el pa página. 
// document.write("El color del coche es "+coche.color);

// Metodo para ver el código dentro de un elemento con un "id" seleccionado.
// document.getElementById("codigo").innerHTML = coche.color +"<br>"+coche.potencia;

/* Metodo para ver el código en la consola "F12". Incluyendo solo el nombre del objeto 
   se conocen todos sus propiedades y sus herencias. */
console.log(coche);